const express = require('express');
const app = express();
const port = 8001;

// Middleware setup
// ...

// Routes setup
// ...

// Start the server
app.listen(port, () => {
  console.log(`Server is running on port ${port}`);
});